import assert from 'power-assert'
import Vue from 'vue'
import { trigger } from '../../src/util'


describe('async', () => {
  let el, vm

  beforeEach(() => {
    el = document.createElement('div')
  })

})
