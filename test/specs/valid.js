import assert from 'power-assert'
import Vue from 'vue'
import { trigger } from '../../src/util'


describe('valid', () => {
  let el, vm

  beforeEach(() => {
    el = document.createElement('div')
  })


  context('default', () => {
    describe('each field', () => {
      context('field1', () => {
        it.skip('should be false', () => {
        })
      })

      context('field1', () => {
        it.skip('should be false', () => {
        })
      })
    })

    describe('entire validator', () => {
      it.skip('should be false', () => {
      })
    })
  })
})
