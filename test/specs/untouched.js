import assert from 'power-assert'
import Vue from 'vue'
import { trigger } from '../../src/util'


describe('untouched', () => {
  let el, vm

  beforeEach(() => {
    el = document.createElement('div')
  })

})
